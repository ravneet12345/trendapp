apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: trend-react-servicemonitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: trend-app
  endpoints:
  - port: http
    path: /metrics
    interval: 15s
